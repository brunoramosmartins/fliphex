{
  "name": "fliphex-web",
  "private": true,
  "type": "module",
  "description": "Tests for the browser build. Not published, not a dependency of the page.",
  "scripts": {
    "test": "node test/page.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "pyodide": "0.26.4"
  }
}
