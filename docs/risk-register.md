# Risk Register

Living document, freely editable. Reviewed at every phase close.
Score = Likelihood × Impact, both 1–3.

| # | Risk | L | I | Score | Mitigation | Status |
|---|---|---|---|---|---|---|
| R1 | Rules ambiguity found mid-implementation | 2 | 3 | 6 | Phase 0 deliberately heavy. Any new ambiguity is written into `rules-canonical.md` and resolved before code changes. Two known items remain: `OPEN-1`, `OPEN-2`. | Open |
| R2 | `OPEN-2` resolves badly: the two decks are mirror images, so the seats are not equivalent | 2 | 3 | 6 | Confound for H1. Must be resolved **before** hypotheses lock at end of Phase 2. If it bites, split H1 into H1a/H1b. Needs a photo of both decks' 3-arrow tiles. | Open |
| R3 | Opening branching factor (1450) makes alpha-beta shallow on the full game | 3 | 2 | 6 | Expected, not a surprise. 3×3 is the guaranteed exact target; retrograde endgames cover the other end. Report achieved depth honestly. | Open |
| R4 | 4×4 solver infeasible in Phase 3 | 2 | 2 | 4 | 3×3 is the committed deliverable; 4×4 is a stretch. Report progress if it does not complete. | Open |
| R5 | Factored policy head (adr-005) too weak — cell/tile/rotation are not conditionally independent | 2 | 2 | 4 | Fallbacks pre-specified in adr-005: condition rotation on cell, then flat 1950-logit head. Must be explicitly checked and logged in Phase 4. | Open |
| R13 | Action space is aliased — 1450 root actions reach only **325 distinct positions** (4.46×; 2.28× over a game). Naive MCTS splits one position's visit counts across up to six labels, while the factored head gives each alias its own prior, so aliased positions draw both more prior mass and more exploration for no reason connected to their merit. The training target `π` is read off the split counts, closing the loop. | 2 | 3 | **6** | Measured 2026-08-28 by `scripts/measure_move_collapse.py`; mechanism and consequences in the adr-005 Phase 3 amendment. Phase 4 must expand children **by position, not by action**, and report the measured effect against the naive tree. Note the flat 1950-logit fallback does *not* fix this — only deduplication does. **Mitigated and measured 2026-08-31 (EXP-010).** `az/mcts.py` expands by position; on the 5×3 that is worth **+4.77** pts [+3.47, +6.06] of top-1 optimality at 400 simulations, and **+8.9** pts on the layers where a decision exists. Two corrections to this row's own text: the predicted **DAG does not appear** — sibling-alias merging leaves every child one parent, so the un-pre-decided backup rule has nothing to govern — and the harm is carried by **prior mass**, not by visit-splitting, since a control that keeps every alias and only fixes the priors recovers the whole effect. Score drops 3×3 → **2×3 = 6**: the mechanism is understood and handled on the reduced board, and what remains is that it has never been measured in a real 5×5 self-play loop. | Mitigated, open on the 5×5 |
| R6 | AZ training unstable or non-convergent | 2 | 2 | 4 | Small net, small game. Multiple seeds distinguish intrinsic instability from bad luck. Shrink the network before growing it. | Open |
| R7 | No board symmetries → no data augmentation, so self-play needs more games | 3 | 1 | 3 | Known from Phase 0 (adr-002). Budget more self-play games; do not plan on a Go-style 8× multiplier. | Accepted |
| R8 | Compute constraint on the G7 laptop | 2 | 2 | 4 | Network sized for this hardware. CPU MCTS + GPU inference. Optionally rent cloud time for the final tuned run only. | Open |
| R9 | Solver and AZ disagree on the 3×3 value | 1 | 3 | 3 | Would be a real finding, not a failure. Investigate; it is a paper-track motivator. | Open |
| R10 | Author burnout juggling FLIPHEX + Career OS + PTCG | 2 | 3 | 6 | No dates anywhere. FLIPHEX starts after PTCG closes. Missing a week is not a defeat. | Open |
| R11 | Paper-track pull causes scope creep | 1 | 2 | 2 | Explicit governance: pursuing publication forks the repo; the portfolio repo stays scoped. | Open |
| R12 | Generated docs drift from the engine | 2 | 2 | 4 | `board-geometry.md` and `piece-archetypes.md` are generated by `scripts/build_docs.py`. Phase 1 adds a test asserting the engine matches, and CI re-runs the build to check the working tree is clean. | Mitigated |
