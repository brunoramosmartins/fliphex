# Canonical figures

**Generated by `python -m figures.build --gallery`. Do not edit by hand.**

One figure per hypothesis with a written verdict, plus support. Phase 6
made this directory a named exit criterion with a clause attached —
*every figure regenerates from a tracked artefact* — and
`tests/test_figures.py` checks the clause rather than trusting it.

Rebuild everything with `python -m figures.build`, which needs the
`figures` extra: `pip install -e ".[figures]"`.

## complexity-landscape.png — H4

FLIPHEX in the state-space / game-tree plane, with every cell graded by provenance and the unsourced ones left off rather than filled in.

![H4](complexity-landscape.png)

Built by `figures.fig_complexity_landscape`. Reads:

- `complexity/state_space.py` — computed, not read
- `complexity/game_tree.py` — computed, not read
- `complexity/comparison.py` — computed, not read

## state-space-and-branching.png — support

The layer profile is a hump; the branching profile falls monotonically. The hump is a cell-count effect, not a width one.

![support](state-space-and-branching.png)

Built by `figures.fig_state_space_and_branching`. Reads:

- `complexity/state_space.py` — computed, not read
- `complexity/branching.py` — computed, not read

## h1-first-player.png — H1

Four exhaustive solves all return P1; the shipped board is not decidable and contributes one corroborative interval.

![H1](h1-first-player.png)

Built by `figures.fig_h1_first_player`. Reads:

- `results/exp017-first-player-5x5.json` — tracked artefact

## h2-extra-tile-criticality.png — H2

Extra-tile criticality by layer on the 5x3. A magnitude with no registered threshold, shown as one.

![H2](h2-extra-tile-criticality.png)

Built by `figures.fig_h2_criticality`. Reads:

- `results/exp002-criticality-5x3.json` — tracked artefact

## h3-seeds-and-agreement.png — H3

Both clauses of H3, side by side: five seeds against the prior-free UCT floor, and five champions against the 0.90 bar.

![H3](h3-seeds-and-agreement.png)

Built by `figures.fig_h3_seeds`. Reads:

- `results/exp015-h3-training-5x5.json` — tracked artefact
- `results/exp006-agreement-5x5.json` — tracked artefact

## h5-forced-measures.png — H5

Both halves of H5's locked measure are fixed by the rules, and the unforced alternative is stratified by rotation-orbit size.

![H5](h5-forced-measures.png)

Built by `figures.fig_h5_forced`. Reads:

- `results/exp017-first-player-5x5.json` — tracked artefact
- `complexity/game_tree.py` — computed, not read

## h6-perturbation-lattice.png — H6

Every perturbation H6 names, and what the project's own decision records permit. The set is smaller than the claim.

![H6](h6-perturbation-lattice.png)

Built by `figures.fig_h6_lattice`. Reads:

- `fliphex/variant.py` — computed, not read
